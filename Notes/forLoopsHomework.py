# Homework
# Ask user for number of grades.
# Let them input each grade, e.g. 5 grades = 5 inputs
# Print grades, 2 FOR loops: one to read in, one to read out.

myGrades=float(input('How many grades do you have: '))